# Lightweight universal failure handling

## Build instructions
- Use cmake with ninja using the `build` directory

## Test instructions
- Use ctest
- Test C++ 11 and C++ 20 by changing the cmake variable `CMAKE_CXX_STANDARD` during configuration.

## Code style
- Keep code compatible with the C++ 11 standard

